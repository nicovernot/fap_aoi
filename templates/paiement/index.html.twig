{% extends 'base.html.twig' %}

{% block title %}Hello PaiementController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

{% if not data %}
<div class="example-wrapper">
    <h1>{{ controller_name }}</h1>

    Vous etes sur le point d'éffectuer un paiement:
    <ul>
        <li>Your controller at <code><a href="{{ '/home/nico/Projects/homecapi/src/Controller/PaiementController.php'|file_link(0) }}">src/Controller/PaiementController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/home/nico/Projects/homecapi/templates/paiement/index.html.twig'|file_link(0) }}">templates/paiement/index.html.twig</a></code></li>
    </ul>
    {{ form(paiementForm) }}
</div>
{% else %}
<script>
var datain =  {{data|json_encode()|raw}};
var jsonform = JSON.stringify(datain);
console.log(datain);
 $.ajax('api/paiementretour', {
    method: "POST",
    contentType: 'application/json',
    processData: false,
    data: jsonform
    })
    
    .then(
    function success(data) {
   
        alert('modification prise en compte');
       
       
       
       
    }
);   
</script>
{% endif %}
{% endblock %}
