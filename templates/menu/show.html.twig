{% extends 'base.html.twig' %}

{% block title %}Menu{% endblock %}

{% block body %}
    <h1>Menu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ menu.id }}</td>
            </tr>
            <tr>
                <th>Mencod</th>
                <td>{{ menu.mencod }}</td>
            </tr>
            <tr>
                <th>Menlib</th>
                <td>{{ menu.menlib }}</td>
            </tr>
            <tr>
                <th>Menord</th>
                <td>{{ menu.menord }}</td>
            </tr>
            <tr>
                <th>Mencom</th>
                <td>{{ menu.mencom }}</td>
            </tr>
            <tr>
                <th>Mendat</th>
                <td>{{ menu.mendat ? menu.mendat|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Menphp</th>
                <td>{{ menu.menphp }}</td>
            </tr>
            <tr>
                <th>Mensql</th>
                <td>{{ menu.mensql }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('menu_index') }}">back to list</a>

    <a href="{{ path('menu_edit', {'id': menu.id}) }}">edit</a>

    {{ include('menu/_delete_form.html.twig') }}
{% endblock %}
